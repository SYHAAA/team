package com.seecen.service;

import com.seecen.domain.Note;

import java.util.List;

public interface INoteService {

    int save(Note note);

    List<Note> list(Note note);

    Note initById(Integer noteId);

    void delete(Integer[] noteIds);

    List<Note> listDelete(Note note);

    void deleteF(Integer[] noteIds);

    void callback(Integer noteId);

    void update(Note note);

    List<Note> listCollections(Note note);

    void cancel(Integer noteId);

    void collection(Integer noteId);


    List<Note> share(Note note);
}
